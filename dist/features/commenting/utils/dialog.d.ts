/**
 * Utility functions for dialog confirmations
 */
/**
 * Confirm deletion of a comment
 * @returns True if confirmed, false otherwise
 */
export declare function confirmDeleteComment(): boolean;
/**
 * Confirm deletion of a thread
 * @returns True if confirmed, false otherwise
 */
export declare function confirmDeleteThread(): boolean;
/**
 * Confirm deletion of all comments
 * @returns True if confirmed, false otherwise
 */
export declare function confirmDeleteAllComments(): boolean;
